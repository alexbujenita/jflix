spring:

  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true


logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.orm.jdbc.bind: trace

server:
  servlet:
    session:
      timeout: 600m
      cookie:
        secure: false
        same-site: lax
